/// @description Insert description here
// You can write your code in this editor
//switch(estado){
	//case "subindo pra direita":

		//x += lengthdir_x(velocidade,image_angle);
		//y += lengthdir_y(velocidade,image_angle-20);
		
/*		if(x>=(xx+(velocidade*25)) && y<=(yy+(1*45))){
			estado="descendo pra esquerda";
		}
	break;
	
	case "descendo pra esquerda":
		x -= lengthdir_x(velocidade,90);
		y += lengthdir_y(1,90);
		
		if(x<=(xx) && y>=(yy-(1*15))){
			estado="subindo pra esquerda";
		}
	break;
	
	case "subindo pra esquerda":
		x -= lengthdir_x(velocidade,90);
		y -= lengthdir_y(1,90);
		
		if(x<=(xx-(velocidade*25)) && y<=(yy+(1*45))){
			estado="descendo pra direita";
		}
	break;
	
	case "descendo pra direita":
		x += lengthdir_x(velocidade,90);
		y -= lengthdir_y(-1,90);
		
		if(x>=(xx) && y>=(yy-(1*15)) ){
			estado="subindo pra direita";
		}
		break
	}


/*switch(estado){
	case "subindo pra direita":
		x += lengthdir_x(velocidade,image_angle-35);
		y -= lengthdir_y(velocidade,image_angle-35);
		
		if(x>=(xx+(velocidade*25)) && y<=(yy+(velocidade*45))){
			estado="descendo pra esquerda";
		}
	break;
	
	case "descendo pra esquerda":
		x -= lengthdir_x(velocidade,image_angle-35);
		y += lengthdir_y(velocidade,image_angle-35);
		
		if(x<=(xx) && y>=(yy-(velocidade*15))){
			estado="subindo pra esquerda";
		}
	break;
	
	case "subindo pra esquerda":
		x -= lengthdir_x(velocidade,image_angle-35);
		y -= lengthdir_y(velocidade,image_angle-35);
		
		if(x<=(xx-(velocidade*25)) && y<=(yy+(velocidade*45))){
			estado="descendo pra direita";
		}
	break;
	
	case "descendo pra direita":
		x += lengthdir_x(velocidade,image_angle-35);
		y -= lengthdir_y(-velocidade,image_angle-35);
		
		if(x>=(xx) && y>=(yy-(velocidade*15)) ){
			estado="subindo pra direita";
		}
		break
	}


	var xx = lengthdir_x(dist, rot + i*ang);
	var yy = lengthdir_y(dist, rot + i*ang);
	filhos[i].image_angle = (i* ang +90)+rot;
	filhos[i].x= x + xx;
	filhos[i].y= y + yy;
}
*/